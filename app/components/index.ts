// Export all components from a single entry point for clean imports

// Layout components
export { SectionWrapper } from "./layout/SectionWrapper";
export { Section } from "./layout/Section";

// UI components
export { UIElement } from "./ui/UIElement";
export { UIGridOverlay } from "./ui/UIGridOverlay";

// Example usage:
// import { SectionWrapper, Section, UIElement } from '@/components';
// OR
// import { SectionWrapper, Section, UIElement } from '@/components/index';
